{
  "name": "ts-project",
  "version": "1.0.0",
  "description": "TypeScript project template with ESLint, Prettier, Vitest and Husky",
  "type": "module",
  "scripts": {
    "dev": "tsx ./src/main.ts",
    "start": "node ./build/main.js",
    "build": "rimraf ./build && tsc --project ./tsconfig.build.json -p ./tsconfig.build.json && tsc-alias -p ./tsconfig.build.json",
    "type:check": "tsc --noEmit --project ./tsconfig.json && tsc --noEmit --project ./tsconfig.test.json",
    "lint": "eslint . --cache",
    "lint:ci": "eslint . --cache --max-warnings=0",
    "lint-staged": "lint-staged",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "test": "vitest run --passWithNoTests",
    "test:ci": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "check": "yarn format:check && yarn lint && yarn type:check && yarn test",
    "check:ci": "yarn format:check && yarn lint:ci && yarn type:check && yarn test:ci",
    "prepare": "husky"
  },
  "private": true,
  "packageManager": "yarn@4.13.0",
  "keywords": [],
  "author": "Eyder Rios",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "dotenv": "^16"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^4.1.0",
    "@vitest/eslint-plugin": "^1.6.12",
    "eslint": "^9",
    "eslint-config-prettier": "^9",
    "eslint-import-resolver-typescript": "^3",
    "eslint-plugin-import": "^2",
    "eslint-plugin-n": "^17",
    "eslint-plugin-promise": "^7",
    "husky": "^9.1.7",
    "lint-staged": "^15",
    "prettier": "^3",
    "rimraf": "^6.1.3",
    "tsc-alias": "^1",
    "tsconfig-paths": "^4",
    "tsx": "^4.21.0",
    "typescript": "^5",
    "typescript-eslint": "^8.57.1",
    "vitest": "^4.1.0",
    "vitest-mock-extended": "^3.1.0"
  }
}
